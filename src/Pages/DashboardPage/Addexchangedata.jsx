import { Typography } from '@mui/material';
import { useParams } from 'react-router-dom';

const Addexchangedata = () => {
    const { id } = useParams();

    return (
        <div>
            <Typography color={'black'}>dataaaaaaaaaaa</Typography>
        </div>
    );
};

export default Addexchangedata;
